abstract class PresentConstants {
  static const resetDuration = Duration(seconds: 2);
  static const snackBarFastDuration = Duration(milliseconds: 2500);
  static const snackStandardDuration = Duration(milliseconds: 3500);
  static const snackLongDuration = Duration(milliseconds: 6000);
}
